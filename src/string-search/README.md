# String Search

Create a function called `stringSearch` that counts the number of times a given substring occurs within the given string.

## Examples

```js
stringSearch('asdf', 'sd'); // returns 1
stringSearch('asdfasdf', 'sd'); // returns 2
stringSearch('hello', 'll'); // returns 1
stringSearch('helo', 'll'); // returns 0
```
